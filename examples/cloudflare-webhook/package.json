{
  "name": "cloudflare-webhook",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "deploy": "npx wrangler deploy -c ./wrangler.toml",
    "types": "npx wrangler types ./src/types.d.ts",
    "secrets": "npx wrangler secret:bulk secrets.json -c ./wrangler.toml --name patreon-webhook"
  },
  "author": "ghostrider-05",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "4.20240405.0",
    "node-html-markdown": "^1.3.0",
    "wrangler": "^3.61.0"
  }
}
